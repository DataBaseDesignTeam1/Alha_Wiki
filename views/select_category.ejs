<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <script src="/static/jquery-3.4.1.min.js"></script>
    <script src="/static/SelectCategoryScript.js"></script>
    <title>Alba_Wiki</title>
    <style>
        html,
        body {
            background: linear-gradient(to top, yellow, #8b00ff);
            background-attachment: fixed;
        }

        #main {
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        #MainHeader {
            background-color: powderblue;
            box-shadow: 0 0 0 0.1rem powderblue;
            border-radius: 12px;
            margin: 10px;
            padding-top: 10px;
            display: flex;
            flex-direction: column;
        }

        #BigCategory {
            background-color: powderblue;
            box-shadow: 0 0 0 0.1rem powderblue;
            border-radius: 12px;
            margin: 10px;
            padding-top: 12px;
        }

        #SmallCategory {
            display: flex;
            flex-direction: row;
            height: 80%;
        }

        div {
            text-align: center;
            flex-grow: 1;

        }

        .SmallCategorys {
            flex-grow: 1;

            display: flex;
            flex-direction: column;

        }

        .SmallCategoryItem {
            background-color: powderblue;
            flex-grow: 1;
            padding-top: 18%;
            margin: 10px;

            box-shadow: 0 0 0 0.1rem powderblue;
            border-radius: 12px;
        }

        select {
            padding: 3px;
            margin: 2px;
            flex-grow: 1;
        }

        #Login {
            float: Right;
            margin-right: 15px;
            padding-right: 10px;
        }

        #UserId {
            float: Right;
            margin-right: 15px;
            padding-right: 10px;
            text-align: right;
        }

        #LogOut {
            margin-left: 15px;
        }
    </style>
</head>

<body>
    <div id="main">
        <div id="MainHeader">
            <div>
                <h1>AlBA WIKI</h1>
            </div>
        </div>
        <div id="BigCategory">
            <h1 id="BigCategoryName">
                <%= bigCategory.data[0].name%>
            </h1>
        </div>
        <div id="SmallCategory">
            <% for(var i = 0; i < small_categories.data.length; i++) { %>
            
            <% if(i % 3 == 0) {%>
                <% if(i != 0) {%>
                </div>
                <% } %>
                <div class="SmallCategorys">
            <% } %>

                <div class="SmallCategoryItem" OnClick="location.href = `http://localhost:4000/category/<%= bigCategory.data[0].index%>/<%= small_categories.data[i].index%>`">
                    <%= small_categories.data[i].name %>
                </div>

            <% } %>
    </div>
    </div>
</body>

</html>