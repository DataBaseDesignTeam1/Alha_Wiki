<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <script src="/static/jquery-3.4.1.min.js"></script>
    <script src="/static/SelectCategoryScript.js"></script>
    <title>Alba_Wiki</title>
    <style>
     html, body {
            background: linear-gradient( to top,#FAF58C, #8b00ff );
            background-attachment: fixed;
        }
        #main {
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction : column;
        }
        #MainHeader {
            background-color: #FF9D6E;
            margin: 0px;
            padding-top: 10px;
            display: flex;
            flex-direction: column;
            height: 45px;
        }


        #BigCategory {
            box-shadow: 0 0 0 0.1rem powderblue;
            border-radius: 12px;
            margin: 30px;
            padding-top: 12px;
            background-color: transparent;
            border: 5px solid #EBF5FF;
        }


        #SmallCategory {
            display: flex;
            flex-direction : row;
            height: 80%;
        }
        div {
            text-align: center;
            flex-grow: 1;
        
        }
        .SmallCategorys {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            height: 80px;
        }
        .SmallCategoryItem {
            line-height: 80px;
            margin: 10px;

            padding: 0px 0px 0 160px;

            box-shadow: 0 0 0 0.1rem powderblue;
            border-radius: 12px;
            display: flex;
            flex-direction: columns;
            white-space: normal;
            flex-grow: 1;
            font-size: 1.5rem;
            background-color: transparent;
            border: 5px solid #EBF5FF;
        }
        select {
            padding : 3px;
            margin: 2px;
            flex-grow: 1;
        }
       #Login {

            margin-bottom: 15px;
            margin-right: 20px;
            padding-right: 12px;
            font-size: 22px;
            background-color: transparent;
            color: #FFFFFF;
            border: 2px solid #FFFFFF;
        }
        #UserId {
            float: Right;
            margin-right: 15px;
            padding-bottom: 70px;
            padding-right: 70px;
            text-align: right;
        }
        #LogOut {
            margin-left: 15px;
        }
        
    </style>
</head>

<body>
    <div id="main">
        <div>
            <h1 id="MainTitle" style="float:left; color:#FFFFFF; padding:15px 2px 0 30px; font-size:40px; font-weight:bold;">AlBA WIKI</h1>
        </div>
        <div id="MainHeader">
        </div>
        <div>
                <div style="float:left; color:#FFFFFF; padding:20px 2px 0 30px; font-size:40px; font-weight:bold;">상세 카테고리 검색하기</div>
            </div>
        <div id="BigCategory">
            <h1 id="BigCategoryName">
                <%= bigCategory.data[0].name%>
            </h1>
        </div>
        <div id="SmallCategory">
            <% for(var i = 0; i < small_categories.data.length; i++) { %>
            
            <% if(i % 3 == 0) {%>
                <% if(i != 0) {%>
                </div>
                <% } %>
                <div class="SmallCategorys">
            <% } %>

                <div class="SmallCategoryItem" OnClick="location.href = `http://localhost:4000/category/<%= bigCategory.data[0].index%>/<%= small_categories.data[i].index%>`">
                    <%= small_categories.data[i].name %>
                </div>

            <% } %>
    </div>
    </div>
</body>

</html>