<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <script src="/static/jquery-3.4.1.min.js"></script>
    <script src="/static/SelectBusinessScript.js"></script>
    <title>Alba_Wiki</title>
    <style>
        html, body {
            background: linear-gradient( to top,#FAF58C, #8b00ff );
            background-attachment: fixed;
        }
        #main {
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction : column;
        }
        #BusinessTable {
            display: flex;
            flex-direction : row;
            height: 80%;
        }
        #MainHeader {
            background-color: powderblue;
            box-shadow: 0 0 0 0.1rem powderblue;
            border-radius: 12px;
            margin: 10px;
            padding-top: 10px;
            display: flex;
            flex-direction : column;
        }
        div {
            border: 1px;
            text-align: center;
            flex-grow: 1;
        
        }
        .BusinessList {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            
        }
        .BusinessItem {
            background-color: powderblue;
            
            padding-top: 13%;
            margin: 10px;
            box-shadow: 0 0 0 0.1rem powderblue;
            border-radius: 12px;
            display: flex;
            flex-direction: columns;
            white-space:normal;
        }
        select {
            padding : 3px;
            margin: 2px;
            flex-grow: 1;
        }
        #Login {
            float: Right;
            margin-right: 15px;
            padding-right : 10px;
        }
        #UserId {
            float: Right;
            margin-right: 15px;
            padding-right : 10px;
            text-align: right;
        }
        #LogOut {
            margin-left: 15px;
        }
        
    </style>
</head>
<body>
    <div id = "main">
        <div>
            <h1>AlBA WIKI</h1>
        </div>
        <div>
            <div style="float:left; color:#FFFFFF; padding:20px 2px 0 30px; font-size:40px; font-weight:bold;">상세 위치 검색하기</div>
        </div>
        <div id= "MainHeader">
            <h1 id = "CategoryName">
                
            </h1>
        </div>
        <div id = "BusinessTable">
            <% for(var i = 0; i < businesses.data.length; i++) { %>
            
                <% if(i % 3 == 0) {%>
                    <% if(i != 0) {%>
                    </div>
                    <% } %>
                    <div class="BusinessList">
                <% } %>
    
                    <div class="BusinessItem" OnClick="location.href = `http://localhost:4000/category/<%= indexes.data.big_category_index%>/<%= indexes.data.small_category_index%>/<%= businesses.data[i].business_index%>`">
                        <%= i+1 %>
                        <%= businesses.data[i].business_name %>
                        <%= businesses.data[i].business_state + businesses.data[i].business_city + businesses.data[i].business_detail_address %>
                    </div>
        
            <% } %>
        </div>
    </div>
</body>
</html>